Data Types:
primitive and non primitive
Primitive datatypes are datatypes which can store only one value
Non Primitive datatyeps can store multiple values (arrays)
int, str, float, bool, double

Linear Search

Two Pointers (binary search):
2 pointers
L and R
mid = (L + R)/2


mistakes:
int left = arr[0];

Linked List:
LL contains head and tail
but tail is used to iterate back ie. to the previous node
but then there is no rev addr in a SLL and hence there is no point in having in SLL cause even if we have it, we wont be able to go to the previous node    

if we want to iterate then we use LL instead of arrays or arrayList
if we want to access an element at a particular index/location, we use array or arrayList

arrayList me kya hota is, say we have 3 elements so array is of size 3
if i want to add one more element, then the machine will take those 3 elemtns, copy it in another place, and then add extra scpace.
but in linked list, all this copying and shit dosent happen